version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build"
    build:
      commands:
        - echo "Build completed - static files ready"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []